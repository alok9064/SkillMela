<div id="uploaded-photos" class="job-page-upload-photos">
              <% if (photos && photos.length > 0) { %>
                <h2>Uploaded Photos</h2>
                  <% job.photos.forEach(photo => { %>
                      <img src="/uploads/<%= photo %>" alt="Photo" width="200px" style="margin: 10px;">
                  <% }) %>
              <% } else { %>
                  <p>No photos uploaded yet.</p>
              <% } %>
          </div>

......................................          

<div class="frl-category-bar">
    <% categories.forEach(category => { %>
        <button class="<%= category.active ? 'active' : '' %>" onclick="changeCategory('<%= category.name %>')">
            <%= category.name %>
        </button>
    <% }) %>
</div>
.....................................
<% proposal.skills.forEach(skill => { %>
    <li><a href="#"><%= skill %></a></li>
<% }) %>
...............................
<% if (offersCount > 0) { %>
    <span class="notification-count"><%= offersCount %></span>
<% } %>

..............................

<!-- Communication Section -->
<div class="communication">
    <h3>Messages</h3>
    <div class="messages">
        <% messages.forEach(message => { %>
            <div class="message <%= message.sender === role ? 'sender' : 'receiver' %>">
                <div class="message-content"></div>
                    <%= message.text %>
                    <span><%= message.sent_at %></span>
                </div>
            </div>
        <% }); %>
    </div>
    <form action="/project/send-message" method="POST">
        <textarea name="message" placeholder="Type your message here..."></textarea>
        <button type="submit">Send</button>
    </form>
</div>

.............................................
<!-- Milestones Section -->
<div class="milestones">
    <h3>Milestones</h3>
    <ul>
        <% project.milestones.forEach(milestone => { %>
            <li>
                <strong><%= milestone.name %></strong>
                <span>Due: <%= milestone.due_date %></span>
                <% if (typeof user !== 'undefined' && user.role === 'client') { %>
                    <span>Status: <%= milestone.status %></span>
                <% } else { %>  
                    <form action="/milestone-check" method="post">
                        <label for="status">Mark as Completed</label>
                        <input type="checkbox" name="status" id="">
                        <button type="submit">Save</button>
                    </form>
                <% } %>
            </li>
        <% }); %>
    </ul>
    <% if (typeof user !== 'undefined' && user.role === 'client') { %>
        <button onclick="addMilestone()">Add Milestone</button> 
    <% } %>
</div>
